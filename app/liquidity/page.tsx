"use client";

import Link from "next/link";
import { ArrowLeft, ArrowUpRight, CheckCircle2, Settings2, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { useDeposits } from "@/context/DepositContext";

const tiers = [{ percent: 50, title: "Fixed deposits", detail: "Capital preservation and predictable returns.", tone: "bg-[#ff5500]" }, { percent: 30, title: "Liquid / arbitrage funds", detail: "Flexible access with potential tax efficiency.", tone: "bg-[#10b981]" }, { percent: 20, title: "SGBs / Treasury Bills", detail: "Long-term growth or a government-backed reserve.", tone: "bg-[#0a192f]" }];

export default function LiquidityPage() {
  const { account } = useDeposits();
  const [surplus, setSurplus] = useState(1000000);
  return <main className="min-h-screen bg-[#fdfbf7] text-[#0f172a]"><nav className="border-b border-[#e9e8e4] bg-white px-6 py-4 sm:px-10"><div className="mx-auto flex max-w-5xl items-center justify-between"><Link href="/dashboard" className="flex items-center gap-2 text-xs font-bold"><ArrowLeft size={16} /> Back to overview</Link><Link href="/settings" aria-label="Open settings" className="text-slate-500"><Settings2 size={18} /></Link></div></nav><div className="mx-auto max-w-5xl px-6 py-10 sm:px-10"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#ff5500]">Liquidity analysis</p><h1 className="mt-3 font-display text-5xl tracking-[-0.03em]">Your cash, mapped clearly.</h1><p className="mt-5 max-w-3xl text-sm leading-6 text-slate-500">VaultFlow identified surplus above your ₹{account.operatingBaseline.toLocaleString("en-IN")} operating baseline. Power users can diversify that surplus instead of placing everything in one fixed deposit.</p><div className="mt-9 grid gap-4 md:grid-cols-3"><div className="rounded-2xl bg-[#0a192f] p-6 text-white"><p className="text-xs text-slate-400">Strategy amount</p><div className="mt-3 flex items-center gap-2"><span className="text-2xl">₹</span><input aria-label="Strategy amount" value={surplus} onChange={(event) => setSurplus(Number(event.target.value) || 0)} className="w-full bg-transparent font-display text-3xl outline-none" /></div><p className="mt-2 text-xs text-slate-300">Amount available for allocation</p></div><div className="rounded-2xl border border-[#e9e8e4] bg-white p-6"><p className="text-xs text-slate-500">Operating reserve</p><p className="mt-3 font-display text-3xl">₹{account.operatingBaseline.toLocaleString("en-IN")}</p><p className="mt-2 text-xs text-slate-500">Kept available for regular operations</p></div><div className="rounded-2xl border border-[#e9e8e4] bg-white p-6"><p className="text-xs text-slate-500">Strategy type</p><p className="mt-3 font-display text-3xl">50 / 30 / 20</p><p className="mt-2 text-xs text-slate-500">Diversified yield allocation</p></div></div><section className="mt-8 rounded-2xl border border-[#e9e8e4] bg-white p-6 shadow-sm sm:p-8"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-start"><div><p className="flex items-center gap-2 text-xs font-bold uppercase tracking-[0.12em] text-[#ff5500]"><ShieldCheck size={15} /> Multi-tier allocation engine</p><h2 className="mt-3 font-display text-3xl">Diversify the ₹{surplus.toLocaleString("en-IN")} opportunity.</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-500">This is a reviewable strategy for HNWIs and power users. VaultFlow shows the split and amounts first; nothing is invested or transferred automatically.</p></div><div className="flex shrink-0 items-center gap-2 rounded-xl bg-[#fff4ec] px-4 py-3 text-xs font-bold text-[#ff5500]"><CheckCircle2 size={16} /> Ready to review</div></div><div className="mt-7 overflow-hidden rounded-xl bg-[#f8f7f3]"><div className="flex h-4">{tiers.map((tier) => <span key={tier.title} className={`${tier.tone}`} style={{ width: `${tier.percent}%` }} />)}</div><div className="grid gap-3 p-4 sm:grid-cols-3">{tiers.map((tier) => <div key={tier.title} className="border-b border-[#e9e8e4] pb-4 last:border-0 sm:border-b-0 sm:border-r sm:pb-0 sm:pr-4 sm:last:border-0"><p className="text-2xl font-bold">{tier.percent}%</p><p className="mt-2 text-xs font-bold">{tier.title}</p><p className="mt-1 text-[10px] leading-4 text-slate-500">{tier.detail}</p><p className="mt-3 text-sm font-bold text-[#0f172a]">₹{Math.round(surplus * tier.percent / 100).toLocaleString("en-IN")}</p></div>)}</div></div><div className="mt-7 flex flex-col justify-between gap-4 border-t border-[#f0eee9] pt-6 sm:flex-row sm:items-center"><p className="max-w-2xl text-xs leading-5 text-slate-500"><strong className="text-[#0f172a]">Important:</strong> mutual funds and gold can fluctuate and Treasury Bills/SGB eligibility varies. Review tax treatment, risk, liquidity, and product terms with a qualified adviser.</p><Link href="/deposit-workflow" className="inline-flex shrink-0 items-center gap-2 rounded-xl bg-[#ff5500] px-5 py-4 text-xs font-bold text-white">Review deposit portion <ArrowUpRight size={16} /></Link></div></section></div></main>;
}
